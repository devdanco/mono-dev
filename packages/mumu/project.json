{
  "name": "mumu",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/mumu/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/mumu",
        "main": "packages/mumu/src/index.ts",
        "tsConfig": "packages/mumu/tsconfig.lib.json",
        "assets": ["packages/mumu/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "packages/mumu/**/*.ts",
          "packages/mumu/package.json"
        ]
      }
    },
    "release": {
      "executor": "nx-release:build-update-publish",
      "options": {
        "libName": "mumu"
      }
    }
  },
  "tags": []
}

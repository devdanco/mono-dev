{
  "name": "bebe",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/bebe/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/bebe",
        "main": "packages/bebe/src/index.ts",
        "tsConfig": "packages/bebe/tsconfig.lib.json",
        "assets": ["packages/bebe/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "packages/bebe/**/*.ts",
          "packages/bebe/package.json"
        ]
      }
    },
    "release": {
      "executor": "nx-release:build-update-publish",
      "options": {
        "libName": "bebe"
      }
    }
  },
  "tags": []
}
